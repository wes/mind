## What this changes

<!-- What it does for someone using the app, not which files you touched. -->

## How you tested it

<!-- e.g. ./build.sh --run against a real calendar, or MIND_DEMO=9 to watch the escalation. -->

- [ ] Built and ran the app bundle (`./build.sh --run`), not just `swift build`
